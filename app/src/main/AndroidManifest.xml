<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
        INTERNET  — WebView (link shell + Privacy/Support) + AppsFlyer + FCM.
        NETWORK   — connectivity monitor (LinkAuditor) reads adapter state.
        POST_NOTIFICATIONS — Android 13+ runtime prompt for FCM.
        POST_NOTIFICATIONS + AD_ID: AppsFlyer 6.16+ requires AD_ID declaration
        on Google Play submissions; declared explicitly here.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />

    <application
        android:name=".BlazingJokerApp"
        android:allowBackup="false"
        android:fullBackupContent="@xml/backup_relay_rules"
        android:dataExtractionRules="@xml/data_relay_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:hardwareAccelerated="true"
        android:enableOnBackInvokedCallback="true"
        android:networkSecurityConfig="@xml/network_relay_rules"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.BlazingJoker"
        tools:replace="android:allowBackup,android:fullBackupContent,android:dataExtractionRules"
        tools:targetApi="35">

        <!-- LoadingActivity is the sole launcher entry point. It runs the boot
             pipeline and dispatches to either MainMenuActivity (white game) or
             WebCanvasActivity (gray shell) once the pilot resolves. -->
        <activity
            android:name=".LoadingActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:alwaysRetainTaskState="true"
            android:screenOrientation="fullSensor"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|smallestScreenSize|density|uiMode|layoutDirection|locale|fontScale|keyboard"
            android:theme="@style/Theme.BlazingJoker.Launch">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainMenuActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.BlazingJoker.Fullscreen" />

        <activity
            android:name=".GameActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:theme="@style/Theme.BlazingJoker.Fullscreen" />

        <activity
            android:name=".WebActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:theme="@style/Theme.BlazingJoker" />

        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:theme="@style/Theme.BlazingJoker.Fullscreen" />

        <!-- Gray flow surfaces -->
        <activity
            android:name=".link.ui.WebCanvasActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:configChanges="orientation|screenSize|keyboardHidden|smallestScreenSize|screenLayout|density|uiMode|layoutDirection|locale|fontScale|keyboard"
            android:windowSoftInputMode="adjustResize|stateHidden"
            android:theme="@style/Theme.BlazingJoker" />

        <activity
            android:name=".link.ui.AlertOptInActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:theme="@style/Theme.BlazingJoker.Fullscreen" />

        <activity
            android:name=".link.ui.NoLinkActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:theme="@style/Theme.BlazingJoker.Fullscreen" />

        <!-- Firebase Messaging service handles push (foreground + cold-tap URLs). -->
        <service
            android:name=".link.push.HornFcmService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- FCM default icon + channel — the icon is the flame silhouette. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_link_flame" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="bj_link_horn_v1" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/link_horn_accent" />

    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="tel" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="mailto" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="market" />
        </intent>
    </queries>

</manifest>
